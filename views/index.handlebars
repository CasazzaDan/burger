<h1> EAT THE BURGER </h1>

<h2>Burgers to try</h2>
<ul>
    {{#each burgers}}
        {{#unless devoured}}
            <p>{{id}}. {{burger_name}}
                <span><button class="change-devour" data-id="{{id}}" data-newdevour=true>
                    {{#if devoured}}devoured!{{else}}devour the burger!{{/if}}
                </button></span>
            </p>
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers eaten</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            <p>{{id}}. {{burger_name}}</p>
        {{/if}}
    {{/each}}
</ul>

<form class="create-form">
    <div class="form-group">
        <label for="burg">Burger:</label>
        <input type="text" id="burg" name="name">
    </div>
    <div class="form-gorup">
        <label for="devoured">Devoured?</label><br>
        <input type="radio" name="devoured" value="0" checked>Ready to be Devoured!<br>
        <input type="radio" name="devoured" value="1">Devoured!
    </div>
    <button tyoe="submit">Add a Burger</button>
</form>
<h5>Add a burger you would like to eat</h5>